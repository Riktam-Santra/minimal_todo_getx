import 'package:flutter/material.dart';

final commonBoxDecor = BoxDecoration(
  borderRadius: BorderRadius.circular(20),
  boxShadow: const [
    BoxShadow(
      color: Colors.white,
      offset: Offset(4, 4),
      blurRadius: 1,
      spreadRadius: 0,
    ),
  ],
);
